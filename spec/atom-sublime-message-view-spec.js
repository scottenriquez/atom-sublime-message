'use babel';

import AtomSublimeMessageView from '../lib/atom-sublime-message-view';

describe('AtomSublimeMessageView', () => {
  it('has one valid test', () => {
    expect('life').toBe('easy');
  });
});
